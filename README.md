# website
motoXsyndicate website and racing platform

Cache handling
--------------
HTML pages are set to revalidate on each visit. Local static assets use version 20260719c in their URLs, so browsers fetch new CSS, JavaScript, and images after this deployment while caching unchanged assets efficiently.
